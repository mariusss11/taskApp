import { Link } from "react-router-dom"

const CreateAccount = ():React.JSX.Element => {
    return <Link to="/sign-up" className="create-account">Don't have an account?</Link>
}

export default CreateAccount